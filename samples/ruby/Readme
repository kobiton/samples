1. Install the latest stable release of Ruby
    \curl -sSL https://get.rvm.io | bash -s stable
    rvm install ruby
2. Install bundler: gem install bundler
3. Install all of the required gems: bundle install
4. Run test from terminal: ruby googletests.rb
